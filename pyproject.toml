[tool.uv.workspace]
members = ["packages/*"]

[dependency-groups]
dev = [
    "duckdb>=1.5.3",
    "griffe>=2.0.2",
    "hypothesis>=6.155.2",
    "mimesis>=19.1.0",
    "mutmut>=3.6.0",
    "mypy>=2.1.0",
    "pointblank>=0.24.0",
    "pre-commit>=4.6.0",
    "pyright>=1.1.410",
    "pytest>=9.0.3",
    "pytest-cov>=7.1.0",
    "ruff>=0.15.16",
    "syrupy>=5.3.1",
    "tach>=0.35.0",
    "twine>=6.2.0",
]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "ANN"]
ignore = []

[tool.ruff.lint.per-file-ignores]
"packages/*/tests/*" = [
    "ANN401",  # duck-typed fakes stand in for real driver objects
]

[tool.mypy]
strict = true
python_version = "3.12"

[tool.pyright]
pythonVersion = "3.12"
typeCheckingMode = "strict"
venvPath = "."
venv = ".venv"

[tool.pytest.ini_options]
addopts = "-q --strict-markers --cov=semql --cov-branch --cov-report=term-missing"
testpaths = ["packages"]
# Test tiers (W5/§10.5). Unmarked tests are treated as ``unit`` — the
# fast PR tier runs ``-m 'not integration and not e2e and not bench'``.
# ``--strict-markers`` (above) turns a typo'd marker into an error, not
# a silent no-op selection.
markers = [
    "unit: fast, in-process, no external services (the implicit default)",
    "integration: exercises a real in-process engine/DuckDB oracle",
    "e2e: full stack across process/network boundaries (needs Docker/CI)",
    "bench: performance/latency budget guard (not a correctness assertion)",
    "security: authz/tenancy/injection containment property or case",
]

[tool.coverage.run]
source = ["semql"]
omit = ["*/tests/*", "*/conftest.py"]

[tool.coverage.report]
show_missing = true
skip_covered = false
fail_under = 0

[tool.mutmut]
source_paths = ["packages/semql/src/semql/"]
# Baseline scope (W5/§10.7): the four correctness-critical core modules.
# ``only_mutate`` fnmatches the copied-tree path, so glob the basenames.
only_mutate = [
    "*/semql/compile.py",
    "*/semql/logical.py",
    "*/semql/cnf.py",
    "*/semql/_resolve.py",
]
pytest_add_cli_args_test_selection = ["packages/semql/tests/"]
pytest_add_cli_args = ["-x", "-q", "--no-cov", "-p", "no:tach"]
# KNOWN BLOCKER (W5/§10.7): ``mutmut run`` mutates the four files above
# correctly (verified: "4 files mutated") but cannot yet *score* them.
# mutmut 3.x sandboxes by copying source into ``mutants/`` and shadowing
# ``./``, ``./src``, ``./source`` on sys.path. This is a uv-workspace
# ``packages/*/src/`` layout installed editable, so ``semql.pth`` keeps
# the *real* ``packages/semql/src`` ahead of the mutant copy — the
# mutations are never imported. Baselining needs a mutmut runner that
# repoints the editable install (or a single-src layout); tracked as a
# follow-up rather than reshaping the repo for one tool.
